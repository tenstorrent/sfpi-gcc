# WP0 baseline freeze (2026-08-16, branch agent/generic-macro-planner)

- Toolchain: /localdev/nkapre/sfpi-gcc-macro-planner-build (xg++ 15.1.0,
  pkgversion tenstorrent/sfpi-macro-planner), source worktree
  /localdev/nkapre/sfpi-gcc-macro-planner at WP0 commit
  (e4b974208 + cherry-picks 4e045d31d, 405f007c5 as 5064964ba/eba8f25ae).
- Baseline: 222/222 loadmacro DejaGnu passes before WP0 additions;
  274/274 after (9 new tests, 52 new pass lines).
- manifest-loadmacro-family-20260816.sha256 covers OFF/ON .text for all
  39 loadmacro-*.C files (78 entries).  OFF = dg-options minus
  -mtt-tensix-emit-loadmacro/-mtt-tensix-analyze-loadmacro.
- The three pre-existing oracle entries (live-lreg-bh, seven-wh, six-bh)
  were re-verified byte-identical; nothing was overwritten.
- swap-mod9 source form is a front-end error on both OFF and ON
  (recorded as COMPILE-ERROR-NO-OUTPUT); the mod-9 SFPSWAP routing exists
  only as a descriptor word derived at emission.
- Coverage gap recorded per DESIGN.md WP0(c): the raw face-advance word
  0x37120004 (`.ttinsn`) has ZERO in-tree DejaGnu coverage at WP0.  It is
  exercised only by TT-Metal LLK kernels.  WP1 introduces the typed
  builtin, deletes the raw-word recognizer, and pins the raw word as an
  opaque byte-identical refusal.

# WP1 (GCC side) completion note (2026-08-16)

- WP1 commit: 30d3c6207 on agent/generic-macro-planner (WP0: ce8b04ea1).
- Typed builtin __builtin_rvtt_ttdstface lands; dst_face_advance_asm_p and
  its raw-word whitelist branches are deleted.  TTSETRWC 0,4,8,0,0,4 (x2)
  assembles byte-identically to the raw .ttinsn 0x37120004 pair (0xdc480010).
- New oracle entries: loadmacro-periodic-minmax-raw-face-word-bh (refusal,
  OFF/ON .text IDENTICAL) and loadmacro-periodic-minmax-two-runs-face-bh
  (positive, DIFFERS as expected).  Manifest now 82 entries.
- Family tallies: loadmacro 291, ttdstface 13, ttsetrwc 22, zero failures.
- SFPI wrapper (sfpi::dst_face_advance) and TT-Metal LLK migration are
  deliberately NOT done tonight; external raw-word kernels refuse
  byte-identically until they migrate.

# WP2 completion note (2026-08-16)

- WP2 commit: 08930f61e on agent/generic-macro-planner.
- Generated effect attribute family + rvtt-effects.{h,cc} query API landed;
  dst-iteration opcode lists migrated to subunit queries; face-advance
  recognition now attribute-driven.
- Byte-identity gates: all 158 compilable tensix test .s outputs identical
  pre/post WP2; all 80 .text oracle entries re-verified unchanged.
- Tallies: loadmacro 291, ttdstface 13, ttsetrwc 22, effects-dump 20 (new),
  dst corpus 26; full rvtt.exp 1187 passes / same 15 pre-existing fails.

# Design-doc corrections queued for WP9 (per coordinator, post-WP2)

- DESIGN.md Sec 1.1 assumes a "genrvtt" generator that emits effect data
  into insn-data tables.  No such generator exists: genrvtt-combine only
  emits gimple combine patterns (rvtt-combine.inc from rvtt.gc).  As built,
  the rvtt-cost.md attribute family is the single source of truth and
  gimple passes reach it via rvtt_builtin_subunit (identity-only
  builtin->late-pattern map + constant-attribute accessor on a
  preset-INSN_CODE scratch insn).  DESIGN.md Sec 1.1/8 WP2 should be
  rewritten to match at WP9.
- -mtt-tensix-dump-effects is retained deliberately: it becomes the
  Layer-7 self-check hook.

# WP3 completion note (2026-08-16)

- WP3 commit: 13e600d88 on agent/generic-macro-planner.
- Shared ownership library rvtt-macro-ownership.{h,cc}: Sec-5.1 RTL
  lattice over the WP2 effect API (join = pointwise pessimism, opacity
  poisons everything) + gimple region-scoped loop-hoist front with the
  deferred-entry-edge-split discipline from sdpa-stack interim fix
  1b32104bc (refusal paths never mutate the CFG).
- gimple-rvtt-invariant.cc converted: function-global opacity refusal
  deleted; refusal message 'function has opaque LREG state' preserved
  verbatim for in-region refusals; opaque-live tests intentionally flip
  to positives (asm outside region).
- Oracle store extended to the full invariant-loadi family (manifest
  134 entries): every refusal OFF/ON-identical; positives differ as
  expected; invariant-loadi-barrier-bh recorded as .s identity due to a
  pre-existing gas SFPENCC operand-order quirk (in-tree it is a -S
  compile test; .s streams are OFF/ON identical).
- Corpus gate: 169-file .s comparison vs post-WP2; only the two
  opaque-live outputs changed, both by the intended hoist.
- Tallies: invariant-loadi 52 -> 76 (branch baseline; design's 59
  counted the sdpa-stack suite), focused total 448, full rvtt.exp 1211
  passes / same 15 pre-existing fails.
- No rvtt-macro-tables.* files created (reserved for the parallel WP6
  agent branching from 08930f61e).

# WP4 completion note (2026-08-16)

- WP4 commit: fa79564e3 on agent/generic-macro-planner.
- rvtt-macro-region.{h,cc}: dataflow-closed row slices, vmap isomorphism
  (scratch placeholders equal; rtx_equal_p compares SCRATCH by identity),
  typed pure-RWC separator classification via the WP2 effect API, ambient
  pure-CC-write tolerance (SFPENCC newly audited: cc=write), per-row
  live-through closure, single-BB v1 with structural self-loop marker.
- rtl-rvtt-macro-planner.cc: analysis-only pass spine registered before
  the quarantined pass; new default-off flag
  -mtt-tensix-macro-planner-analyze; flag-ON codegen proven identical.
- New refusal names (append-only): row-opaque-effect, row-not-closed,
  row-cc-write, row-config-write, row-not-isomorphic,
  row-stride-mismatch, row-live-through.
- Gates: corpus .s identical to post-WP3 except the intended SFPENCC
  annotation in the effects-dump golden; 134 oracle entries re-verified;
  focused 481 passes (macro-planner 32); full rvtt.exp 1244 / same 15
  pre-existing fails.
- v1 adaptations for WP9 doc sync: macro_region carries a structural
  self-loop bool (loop metadata unavailable at this pass slot); vmap is
  a reg-pair vec rather than hash_map; rows with external LREG inputs
  refuse as row-not-closed pending the Layer-5 livein obligation.

# WP6-prep cherry-pick + WP5 completion note (2026-08-16)

- Cherry-picks: de1542810 (capability tables, new files, verbatim from
  f974e880a) and efd47e6f3 (t-riscv-tt registration from 7931c19da; ONE
  trivial union conflict in RVTT_OBJS resolved by keeping all objects).
  Standalone table test re-verified: 490 checks / 0 failures (c++11 and
  c++17); compiler links rvtt-macro-tables.o cleanly.
- WP5 commit: 52d258292.  Scheduler derives: minmax = 3-slot
  alternating-VD calendar (ii=3, launch m0 load+hosted simple, explicit
  load, launch m1 delayed store + absorbed Dst+=2) with
  event-delay-unproven refusal; signbit = ii=1 single launch, hosted=3,
  delay-unproven refusal; cast-round = ii=1 fully proven, drain=2, no
  refusal; QSR = target-macro-encoding-unproven.
- WP7-BLOCKING items (per coordinator flagged-constant rule): minmax
  m0/m1 and signbit shift/cast per-event delays (NOTES-wp6-prep.md 9(g))
  block schedule commitment -- resolve against the architectural
  reference; the BH launch addr-mode shift conflict 9(a) is not yet
  consumed by any scheduler path (launch encoding is WP6/WP7).
- New audits: SFPSHFT-immediate (both forms) and SFPCAST subunit=simple
  flagged as NOTES-wp6-prep 9(h)-class inferences in rvtt.md comments.
- New refusal names (append-only): event-delay-unproven,
  sequence-encoding-unproven, template-capacity-exceeded, port-conflict,
  latency-violation.
- Gates: corpus .s byte-identical to post-WP4; analyze-ON codegen
  identical; 134 oracles OK; focused 508 passes; full rvtt.exp 1271 /
  same 15 pre-existing fails; sched-core standalone test 17/0.

# WP6 completion note (2026-08-16)

- WP6 commit: 6604fedf9.  Descriptor synthesis + in-tree verifier,
  analysis-only (WP7 owns emission).
- All frozen descriptor words reproduced DERIVED: minmax BH/WH five
  words incl. mod-1/9 routing from which-SET-reaches-store; signbit four
  words with imm12 packed from the typed shift immediate; cast-round
  four words fully field-packed; SETC16 3 (BH) / 6 (WH dual-slot);
  launch words match every NOTES-wp6-prep reference value.
- Proven-whole-word program matching (keyed by derived event structure +
  admitted unspec->opcode-byte identity) supplies delays/drain and
  un-blocks the minmax/signbit DELAY_UNKNOWN schedule refusals per
  coordinator direction; descriptor-program-unproven remains the named
  refusal for unmatched structures (WP7-blocking with the architectural
  reference).
- Verifier: decode-back cross-check vs independently assembled
  expectations; component-tagged descriptor-verification-failed; forced
  on under checking + -mtt-tensix-macro-planner-verify.  Standalone
  adversarial suite 11/0; table round-trip 490/0 re-verified.
- Design 4.3 patterns landed unused: field-operand rvtt_owned_setc16
  (word packed by tables at output) and generic
  rvtt_sfploadmacro_hidden_int (hidden write = register operand).
- Cast-round drain note: derived greatest-remaining-delay = 2 from the
  documented d0/d1/d2 delays; descriptor drain = proven-calendar 3 (the
  frozen 3-NOP padding).  Divergence recorded for the WP7 parity gate.
- Gates: corpus byte-identical to post-WP5; 134 oracles OK; focused 557;
  full rvtt.exp 1320 / same 15 pre-existing fails.

# WP7 completion note (2026-08-16)

- WP7 commit: 9e1aae005.  End-to-end -mtt-tensix-macro-planner with
  emission; quarantined pass mutually excluded; Min/Max calendar DELETED.
- Parity gate: 19/19 periodic-minmax byte-parities vs frozen oracles
  (formers AND refusals), re-verified post-deletion; 130 oracle entries
  verified with the retargeted planner flag; break-evens 7-BH/8-WH fall
  out of the derived cost model (loadi-half/config/SETC16/drain vs
  explicit row issues, conservative per run); mismatched-modes fixed via
  per-program uniform-mode CRAQ-envelope data.
- CRAQ 8/8 rerun PENDING (agent does not run CRAQ): use the verbatim
  protocol in /localdev/nkapre/wp1-trio-evidence-20260816
  (run_craq_matrix.sh + hybrid-toolchain pattern in PROVENANCE.tsv),
  substituting this branch's compiler at 9e1aae005 with
  -mtt-tensix-macro-planner for the ON leg, kernel
  calculate_binary_max_min_fresh_cpp (test_sfpu_binary.py::
  test_fresh_cpp_binary_max_min), {BH,WH}x{OFF,ON}x{Min,Max}.
- Deviations: emit_owned_setc16 / emit_owned_dst_increment_address_
  modifier / rvtt_owned_setc16_int retained (still consumed by the
  quarantined signbit/cast-round shapes; deleted with them at WP8);
  signbit/select launch packing likewise WP8.
- Gates: focused 557; full rvtt.exp 1320 / frozen FAIL set; corpus .s
  ZERO diffs vs post-WP6 (planner emission byte-identical).

# WP8 status: DEFERRED after intermediate label commit (2026-08-16)

- Landed: e40af0014 (comment-only quarantine header label on
  rtl-rvtt-loadmacro.cc; build re-verified green).
- WP8 proper is NOT attempted in this session, honestly reported rather
  than half-landed: signbit parity requires two new discovery
  capabilities the v1 planner deliberately lacks -- (a) SINGLE-ROW
  regions (the staged-bh/wh tests form one-row regions; discovery
  currently requires >=2 isomorphic rows) and (b) LOOP-BODY regions with
  structural-preheader config hoisting + zero-trip obligations
  (staged-loop-bh/wh) -- before Typecast cross-region sharing (the
  hardest obligation) even starts.  Landing the WP8 deletions without
  those would break the staged/analysis/minmax(4-insn) suites and
  violate the deletions-only-with-a-green-gate rule.
- WP8 worklist for the next session, in order:
  1. discovery: single-row regions + loop-body regions (structural
     preheader via the WP3 ownership library discipline; zero-trip per
     DESIGN 5.5); profitability trip_weight for loops.
  2. signbit parity gate vs loadmacro-staged-*/analysis-* oracles
     (4 SFPCONFIG preheader words, 1 launch/row, drain 3; resolve the
     recorded cast-round drain-2-vs-3 divergence with a generic
     table-driven padding rule -- parity wins).
  3. cast-round parity (loadmacro-staged U16->BF16 shape; mode envelope
     per-program as done for binary at WP7).
  4. typecast cross-region sharing (four typed-TTSETRWC face regions,
     WH bank-base dual-slot proof; CRAQ recipe: same wp1-trio protocol,
     kernel calculate_typecast_uint16_to_fp16b_semantic with the typed
     TTSETRWC discriminator f3b5bba3 -- record-only, do not run).
  5. Where positives -> cc-template-unsupported refusals; delete
     rtl-rvtt-loadmacro.cc entirely + pass registration + old flags
     (refuse as errors); re-verify the 19 minmax parities and the full
     oracle store afterwards.
- All WP7 gates remain the branch state of record: 19/19 parities, 130
  oracles, focused 557, full rvtt.exp 1320 / frozen FAIL set.

# In-place-store coverage-gap fix note (2026-08-16, post-WP8-label)

- Gap (found by the WP7 CRAQ matrix, planner-craq-evidence-20260816): the
  production Min/Max kernel stores IN PLACE (store Dst address == first
  load's), carrier grouping merged the store into the load's launch
  carrier (ii=2), which fails latency and proves no descriptor program ->
  refusal.  All 19 parity tests store to a distinct third address.
- Fix: deterministic two-candidate carrier-grouping search in
  rvtt-macro-sched.cc (candidate 0 maximal sharing; candidate 1 demotes
  every Dst store to its own delayed-store carrier, existing only when
  candidate 0 merged a store); the planner pass accepts the first
  candidate whose descriptor synthesis proves.  No address-equality
  special case; refusal preserved when no candidate proves.
- New oracle entries: manifest-inplace-minmax-20260816.sha256 (22 lines:
  six formation tests off/on/frozen-reference + two analyze tests
  off/on).  All six planner ON .text BYTE-IDENTICAL to the frozen
  quarantined pass (-mtt-tensix-emit-loadmacro, macro-planner-toolchain
  WP1-era compiler with the calendar intact); OFF legs refusal-identical;
  analyze legs mutate nothing.  Includes the production 8-rows-per-face
  shape (rows=32 runs=4, ttdstface separators) on BH and WH and the
  mod-9 second-result routing variants.
- Gates re-verified: full rvtt.exp 1431 passes (+111 new lines from 8 new
  tests) / frozen 15-FAIL set unchanged; corpus A/B (WP7-snapshot
  compiler vs fixed compiler, dg-options) 195 identical / 6 intended
  diffs (the new in-place formation tests only); all 134 prior oracle
  entries reproduced-and-preserved (132 .text three-way + 2 .s identity).
